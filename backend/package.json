{
  "name": "idealcar-backend",
  "version": "1.0.0",
  "description": "Backend API for IdealCar dealership website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"No tests specified\" && exit 0",
    "setup": "cp .env.example .env && echo 'Setup complete! Update .env with your settings.'"
  },
  "keywords": ["car", "dealership", "api", "express"],
  "author": "DirkL",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}